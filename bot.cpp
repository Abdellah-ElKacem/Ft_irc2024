#include "Server.hpp"

void Server::bot(std::string cmd) {
    if (cmd == "!help") {
        char buffer[512];
        while (true)
        {
            int bytes = recv(); 
        }
        
    }
}