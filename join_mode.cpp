#include "Clients.hpp"
#include "channel.hpp"

void pars_cmd(std::vector<std::string> cmd)
{
    std::vector<std::string> name;
    std::vector<std::string> pass;
    if (cmd[0] == "JOIN")
    {
        
    }
}